# Frontend

This directory contains the React/Vite frontend for Invoice Contract OCR Manager.

## Commands

```bash
npm install
npm run dev
npm run build
npm run lint
npm run preview
```

## Notes

The frontend communicates with the local FastAPI backend through `/api/*` endpoints. Runtime documents, archives, databases, and build artifacts should not be committed.
